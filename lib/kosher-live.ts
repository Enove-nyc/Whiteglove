/**
 * One sentence about the live kosher finder, said the same way everywhere.
 *
 * THE FINDER IS THE SITE'S MOST USEFUL FREE TOOL AND ITS EASIEST THING TO
 * MISREAD. It draws restaurants, bakeries and groceries live from
 * OpenStreetMap — a map anybody can edit — so it is current, worldwide, and
 * carries no supervision information whatsoever. /kosher-travel says all of
 * that properly. What it could not do was travel with the claim: the front
 * page offered "restaurants, bakeries and groceries, live, anywhere in the
 * world" with nothing beside it, which is the sentence a traveller acts on.
 *
 * So the caveat is a constant rather than a paragraph retyped per page, for
 * the reason the affiliate disclosure is: a caveat that is wrong in one of six
 * places is worse than none, because it proves the site knew it owed one.
 *
 * SHORT goes beside a promotion — a card, a footer line, a heading. LONG goes
 * where somebody is about to rely on a result. Neither is a status report about
 * our own work: both describe where the data comes from and what the traveller
 * should do with it, which is what they need either way.
 */

/** Beside a link or a claim. One line, no heading. */
export const LIVE_FINDER_SHORT =
  "Live results come from OpenStreetMap, which anyone can add to — a lead to check, not a hechsher.";

/** Beside the results themselves, where somebody is about to act on one. */
export const LIVE_FINDER_LONG =
  "These results come live from OpenStreetMap, a map anyone can add to. It is the best way to find out what exists in a town you do not know, and it says nothing about supervision: confirm the hechsher with the place itself, or with the local kehilla, before you eat there.";

/** The customer-facing name for the tool, so the site calls it one thing. */
export const LIVE_FINDER_NAME = "kosher food finder";
