{
  "name": "White Glove",
  "install": "npm ci",
  "ports": [{ "name": "web", "port": 3000 }],
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev",
      "description": "Next.js dev server (Turbopack) on http://localhost:3000. The app runs entirely on bundled static content unless DATABASE_URL / Upstash Redis are set, so no external services are required for local work."
    }
  ]
}
