import Link from "next/link";
import SiteLockControl from "@/components/SiteLockControl";
import { getDashboardStats } from "@/lib/site-analytics";

function RankedList({ title, rows, empty }: { title: string; rows: Array<{ label: string; count: number }>; empty: string }) {
  return <section className="border border-[var(--gold-light)] bg-[#fcfaf6] p-6"><p className="text-xs font-bold uppercase tracking-[0.17em] text-[var(--gold)]">Activity</p><h2 className="mt-2 font-[family-name:var(--font-display)] text-3xl text-[var(--navy)]">{title}</h2>{rows.length ? <ol className="mt-5 divide-y divide-[var(--gold-light)]">{rows.map((row, index) => <li className="flex items-center justify-between gap-4 py-3" key={row.label}><span className="min-w-0 truncate text-stone-700"><span className="mr-3 text-xs font-bold text-[var(--gold)]">{index + 1}</span>{row.label}</span><strong className="text-[var(--navy)]">{row.count}</strong></li>)}</ol> : <p className="mt-5 text-sm leading-6 text-stone-600">{empty}</p>}</section>;
}

export default async function AdminPage() {
  const stats = await getDashboardStats();
  return <main className="min-h-screen bg-[var(--cream)]"><header className="border-b border-[var(--gold-light)]"><div className="mx-auto flex max-w-6xl items-center justify-between gap-5 px-5 py-6 sm:px-8"><div><p className="text-xs font-bold uppercase tracking-[0.22em] text-[var(--gold)]">White Glove Itineraries</p><h1 className="mt-2 font-[family-name:var(--font-display)] text-4xl text-[var(--navy)]">Owner&apos;s dashboard</h1></div><Link href="/" className="border border-[var(--gold)] px-4 py-3 text-xs font-bold uppercase tracking-[0.12em] text-[var(--navy)]">View website</Link></div></header><section className="mx-auto max-w-6xl px-5 py-10 sm:px-8"><div className="grid gap-5 md:grid-cols-3"><div className="border border-[var(--gold-light)] bg-[#fcfaf6] p-6"><p className="text-xs font-bold uppercase tracking-[0.17em] text-[var(--gold)]">All recorded visits</p><p className="mt-3 font-[family-name:var(--font-display)] text-5xl text-[var(--navy)]">{stats.visits}</p><p className="mt-3 text-sm leading-6 text-stone-600">One visit is counted once per person&apos;s browser session on each page.</p></div><div className="border border-[var(--gold-light)] bg-[#fcfaf6] p-6"><p className="text-xs font-bold uppercase tracking-[0.17em] text-[var(--gold)]">Searches tracked</p><p className="mt-3 font-[family-name:var(--font-display)] text-5xl text-[var(--navy)]">{stats.topSearches.reduce((total, item) => total + item.count, 0)}</p><p className="mt-3 text-sm leading-6 text-stone-600">Use this to see which places deserve the most complete updates.</p></div><div className="border border-[var(--gold-light)] bg-[#fcfaf6] p-6"><p className="text-xs font-bold uppercase tracking-[0.17em] text-[var(--gold)]">Data connection</p><p className="mt-3 font-[family-name:var(--font-display)] text-3xl text-[var(--navy)]">{stats.configured ? "Connected" : "Not connected"}</p><p className="mt-3 text-sm leading-6 text-stone-600">{stats.configured ? "Visitor activity is being saved privately." : "Connect the private analytics store to begin collecting real traffic."}</p></div></div><div className="mt-5"><SiteLockControl initialLocked={stats.siteLocked} configured={stats.configured} /></div><div className="mt-5 grid gap-5 lg:grid-cols-2"><RankedList title="Most searched places" rows={stats.topSearches} empty="No searches have been recorded yet. Once connected, each completed destination search will appear here." /><RankedList title="Most visited pages" rows={stats.topPages} empty="No page visits have been recorded yet. Once connected, you will see the pages visitors use most." /></div><section className="mt-5 border border-[var(--gold-light)] bg-[#fcfaf6] p-6"><p className="text-xs font-bold uppercase tracking-[0.17em] text-[var(--gold)]">One-time setup</p><h2 className="mt-2 font-[family-name:var(--font-display)] text-3xl text-[var(--navy)]">Private settings to add before launch</h2><ul className="mt-4 list-disc space-y-2 pl-5 text-sm leading-6 text-stone-600"><li><code>ADMIN_PASSWORD</code> — only you use this to open this dashboard.</li><li><code>SITE_ACCESS_PASSWORD</code> — the password visitors use while the site is private.</li><li><code>WHITE_GLOVE_SESSION_SECRET</code> — a long, private random phrase that keeps the access cookies secure.</li><li><code>UPSTASH_REDIS_REST_URL</code> and <code>UPSTASH_REDIS_REST_TOKEN</code> — the private connection that saves traffic, searches, and the live lock setting.</li></ul></section></section></main>;
}
