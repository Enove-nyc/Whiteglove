# Capacitor deps are not committed — CI reinstalls them from package.json.
# The android/ project IS committed, because it now carries native
# customizations (microphone/camera/location permissions, system-bar colors)
# that must survive every build.
node_modules/
