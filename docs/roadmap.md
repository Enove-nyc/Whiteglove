# Roadmap

Two review scans, in one list, with what has actually been built marked as
such. Nothing here is marked done unless it is done and was checked.

Status words mean exactly this:

- **Done** — built, tested, merged.
- **Part** — some of it is built; the rest is named.
- **Blocked** — cannot start without a decision or a database migration.
- **Open** — not started.

---

## Scan 1 — public site review

| # | Item | Status | Where |
| --- | --- | --- | --- |
| 1 | Consolidate `/book` and `/booking` | **Done** | #145 |
| 2 | Real phone and tablet testing, fix breakpoints | **Done** | #147 · `npm run audit:ui` |
| 3 | Form labels, focus states, touch targets | **Done** | #147 |
| 4 | One destination schema and public template | **Part** | template already shared; the extra fields need the migration below |
| 5 | Completeness and verification workflow in the admin | **Done** | #149 |
| 6 | Finish Uman and the most-visited guides | **Blocked** | needs the migration below |
| 7 | Connect destination pages to routes and itineraries | **Done** | #150 |
| 8 | Unique metadata, canonicals, social cards, structured data | **Done** | #151 — **needs `NEXT_PUBLIC_SITE_URL` set** |
| 9 | Account verification, recovery, rate limiting | **Done** | #152 |
| 10 | Route optimization, saving, sharing, printing | **Part** | #153 — map preview and drag-and-drop still open |
| 11 | Directory filtering and large-list performance | **Done** | #154 |
| 12 | Provider verification and listing management | **Done** | a number is published only on consent or a public source of their own; a checked date and response time show when set; both admin screens can record consent. **Needs the migration run.** |
| 13 | Sponsored-content experience | **Open** | |
| 14 | Performance, accessibility and cross-browser QA | **Part** | audit script exists; Safari still needs a real device |

---

## Scan 2 — admin and Travel Command Center

The full text of this scan is the specification. What follows is its 27
sections with an honest status, so nothing is quietly assumed to be done.

### Already built, in whole or in part

| Section | Status | Note |
| --- | --- | --- |
| 9. Destination completion tracker | **Done** | admin work queue, 297 records sorted thinnest-first, per-record missing-field list. The public side shows words, never a percentage. |
| 13. Border crossings in the public planner | **Part** | the planner worked driving time out from the road and then said, separately and further down the page, that the border can take hours — so the arithmetic and the warning disagreed, and a day planned around the five hours ended at the barrier. **The border is now counted inside the driving time**, and inside the arrival times with it: a Lizhensk-to-Uman day that read as 9.2 hours reads as 11.2, ends at 22:11 instead of 20:11, and raises the over-packed notice it should always have raised. Three things, never confused: a crossing somebody checked recently with a figure on it is a **measurement**, used and attributed by name; an unchecked external border gets a stated **allowance** that says it is one; inside Schengen nothing is added at all, because padding every internal border would make the day wrong the other way. A stale check falls back to the allowance — a queue length from four months ago is not a fact about today. The figure is a separate number in the admin from the words a traveller reads: "about two hours for a coach, less before six" is what somebody needs to see, and no parsing turns it into arithmetic worth trusting. **Still open:** live wait-time data from anywhere but the owner. |
| 14. Public intelligent route planner | **Part** | fixed start/end, optimisation between them, manual reordering, driving times, save, share, print. Missing: cost/fuel/tolls, avoid-tolls/ferries, along-the-route food and minyanim, PDF export. |
| Trip countdown and follow-along (owner request, not in either scan) | **Done** | the planner laid the days out and then said the same thing on the morning of the flight as it did four months earlier — and the same thing again on the third morning in Kraków, when the only question anybody has is "where am I meant to be now". Every trip now carries its own age at the top: **a countdown while it is ahead** ("20 days to go", "Tomorrow"), and once it starts, **the day you are on and the stop you should be at** — what is happening now, what comes after it, how many of today's stops are behind you, and where you are sleeping tonight. The day you are on is the one that opens, marked TODAY, instead of day one. The trip list carries the short version, so a trip that leaves tomorrow says so without being opened. **The clock is the traveler's own device**, not the server: somebody in Poland at one in the morning is on a different date from a server in UTC, and that is exactly the moment this has to be right. A stop the planner could not work a time out for is listed and never placed on the clock — inventing one would send somebody to a kever the plan never said they would be at. **Still open:** anything that pushes rather than waits to be opened, and the same strip on the shared and printed views. |
| 23/24. What changed — history and undo | **Part** | the recycle bin covers a deletion. It does not cover the quieter way to lose something: saving over it. A digit retyped wrong in a shomer's number, a short overview pasted over a long one — and there was no record anywhere that the page had ever said anything else, and no answer to "who changed this". `/admin/history` now holds every edit of the last 30 days: **what it said before, what it says now, and who did it** — a name when they signed in as themselves (§18), "the shared password" when that is the truth. Put any of it back without touching anything else: only the fields that changed are written back, so undoing a phone number at noon does not take back the note somebody fixed at eleven. **Only real differences are recorded.** A form save writes every field it holds, so logging "the save" would say fourteen things changed when one digit did; a save with nothing different in it is not recorded at all. Covers towns, pages, listings, contacts, shomer numbers, kevarim and businesses. **Still open:** scheduling a change for a date, and a history that goes back further than a month. |
| A place a traveller found and we have not got (owner request, not in either scan) | **Done** | travellers find things the site has not reached — a hotel in a town with two, a museum worth an afternoon, a shul that keeps a key. Every one was typed into somebody's itinerary and went nowhere, and the next person planning the same town started from the same blank. Now, when a stop or a place to stay is added that the site does not hold, **the traveller is asked whether they would send it in** — and the answer, either way, goes to the owner's existing suggestions queue as a "new entry request", so there is no second list to remember to check. **Asked, not taken.** An itinerary is a private document: somebody's dates, the notes they wrote to themselves, who they are travelling with, the reference for their hotel. Reading it and turning parts of it into website content because it happens to sit on our server would be using something handed over for one purpose for another, which is what data protection law is about and is wrong whether or not a law reaches it. So nothing leaves without a press, and **the notice shows its working**: every field that would go is listed with its real value, and what would not go is written out underneath — the dates, the notes, the trip's name, who else is on it, any booking reference or boarding pass. Both lists come from one function, so the screen cannot promise one thing and post another; a test builds the whole payload from a trip seeded with unmistakable private values and fails if any of them appear. **The phone number is named on its own**, because a number typed on a stop is as likely to be a friend's as a hotel's. **A no is final** and is remembered by the place rather than the stop, so the same hotel on next year's trip does not ask again. Nothing is offered for something that is not a place ("Lunch", "Drive to Kraków"), for somewhere the site already has, or to somebody not signed in — a suggestion needs a name to credit, and collecting an address through a pop-up to get one would be worse than not asking. The check that decides whether to ask **sends only a name**. **Still open:** the same offer for a trip built before this existed, rather than only at the moment of adding. |
| THE DIRECTORY WAS DELETED BY THE RE-IMPORT AND NEVER PUT BACK (owner report) | **Done** | the real cause, found by running the import against a real database and counting the rows afterwards. `seedDatabase` **deleted all thirty built-in businesses by slug and had no `directoryProvider.createMany` at all** — every other kind in the seed had its create; this one alone did not. And `countSeedRows` still reported thirty, so the button said **"imported 30 directory listings" immediately after destroying them**. Measured: before the fix the import reported 30 and left the table with 0 of them; after it, 1 → 31. **The guard is a test that compares the models the re-import deletes with the models it creates**, and fails on any that is deleted and never written back — checked against the version on main, where it names `directoryProvider` and fails. Alongside it: a re-import legitimately restores a built-in business to the shipped text, and it used to do that without a trace, so the previous values of every one it overwrites are now written to `/admin/history` **before** the delete, and the result says how many carried the owner's own wording and where to find it. |
| The businesses editor said 0 while the directory showed 30 (owner report) | **Done** | the second half of the same report, and the actual cause. `/admin/directory/businesses` reads **only the database**, and the thirty businesses in `data/directory.ts` have no row — so an owner with an empty table saw an empty list and the words "Pick a provider or add a new one", **while every visitor to /directory was being shown thirty businesses he had no screen for, could not see, could not edit and could not remove.** Two numbers disagreeing with nothing anywhere to reconcile them. **And the two sets are all-or-nothing**: the built-in list is a fallback, not a base layer, so the moment one row exists in the database all thirty disappear from the public site — adding a single business silently takes thirty off the directory, which is exactly what a vanished directory looks like from the outside. The editor now lists **both**, each built-in one marked, with a count that reads `0 yours · 30 built in`, and says in a sentence which set a visitor is actually being shown and what will happen when the first of his own is added. The all-or-nothing behaviour is unchanged and now **visible**; whether the built-in thirty should instead layer under his own, the way added airports do, is a decision for the owner. Verified against a real PostgreSQL in both states. |
| The business directory vanished, and nothing said why (owner report) | **Done** | reported as a lost directory of contacts. **Nothing was lost.** `getPublicProviders` had three separate paths that fell back to the thirty businesses shipped in `data/directory.ts` — no `DATABASE_URL`, the query returning nothing, and the read failing outright — and **all three returned that list in silence**. The owner saw his directory replaced by samples with no error, no notice, and no way to tell which of the three had happened or whether his own entries still existed. The fallback itself is right: these are real businesses, and somebody looking for a driver is better served by thirty than by none. Serving them **as though they were his** was not. The reader now says where its list came from, and `/admin/directory` says so plainly — naming `DATABASE_URL` when there is no database, saying **"your own entries are almost certainly still there"** when the read failed, and pointing at Directory → Businesses when nothing is published, because `listProvidersForAdmin` does not filter by status and shows drafts the directory cannot. It never says anything is lost, because nothing here knows that and saying it would send somebody off to retype a directory sitting in the database. Verified against a real PostgreSQL in all four states. |
| The front page went down when an advert could not be read (found while testing) | **Done** | with the private store unreachable, `/` and `/itinerary` returned a **500**. Every other public page degraded correctly; those two did not, and the reason was an advert — one `fetch` in `lib/admin-content.ts` with no `catch`, the only one like it in the codebase. A blip at Upstash took the front page of the business down. **A read now degrades**: no advert, no visitor counts, and the page renders — an advert that does not appear beats a front page that does not appear. **The careful half is the write.** Every save in that file is read-modify-write over one big value, so catching the failure naively would have been worse than the bug: a read that quietly returned "empty" during the blip, followed by a write that succeeded, would have replaced 297 locations, every listing and every advert with nothing. The hard failure was preventing that by accident. An unreachable store is now told apart from an empty one by name, and **all ten save paths refuse rather than write on top of a bundle they never read** — and say so, instead of the old message telling somebody to connect a database that is already connected, or telling them their suggestion had been deleted when it had not. **The guard is `npm run audit:outage`**: every public page, fetched with the store pointed at a dead port, all of them 200. Nothing in the other audits would have found this — every page is perfectly correct while the store is up. |
| Multi-city was there and nobody could see it (owner report) | **Done** | reported twice as "there is no multi city", and both times the button existed, worked, and was not visible. **On a phone the three trip types were a wrapping row and the third dropped onto a line of its own, below the fold.** What was left on screen — round trip, one way — read as the whole choice, so multi-city looked like something this site could not do. They are a grid of equal thirds now and cannot come apart at any width; the nonstop tick takes its own row on a phone, where there is no width beside them. Measured at 320/375/390/768/1024/1280: all three on one line, every target 44px, no sideways scrolling. Same fix on the on-site Duffel search, which had the identical row. **The guard is a new check in `npm run audit:ui`**: a marked row of choices that comes apart onto two lines is reported. A wrapped row is not overflow and is not an error, so nothing already in the audit would ever have caught it — proved by putting the old layout back in a live page and watching the check fire. |
| The flight search after the move to Kayak (owner request, not in either scan) | **Done** | moving flights back to Kayak took two things with it that the on-site search had, and left a third that was broken all along. **Multi-city and the nonstop filter are back**, alongside round trip and one way; switching between them keeps what has already been typed. **Picking an airport used to lock the field** — the box then held the whole label, "New York — all airports (NYC)", the list matched nothing against it, and reopening showed an empty dropdown with no way to choose a different airport short of deleting the text by hand. It now searches on the code inside the label, so reopening offers that city's airports; and the text is selected when the box is clicked, so typing replaces the pick instead of landing in the middle of it. That second part needed two fixes a browser found and reading did not: **clicking back into an input that never lost focus fires no focus event**, and **selecting before the list opens is undone** when the re-render hands the controlled input its value again. **A half-typed city is no longer taken as an airport code.** "New" is three letters, and the old code matched any three-letter word anywhere in the box — so somebody mid-word would have been sent to a search for a city they never chose, and it would have looked like it worked. Codes are now checked against the airports and metro areas the site actually knows. The whole Kayak URL — one way, round trip, multi-city, nonstop, affiliate key — is built in one tested place, because a wrong one does not throw: it opens Kayak on the wrong day, or in the wrong city, or with the filters silently dropped, and nobody finds out until somebody has booked. |
| Flights go to Kayak (owner request, not in either scan) | **Done** | flights were being searched and booked on this site through Duffel, and nobody had decided that. The rule was `DUFFEL_ACCESS_TOKEN is set && no Kayak affiliate key` — which means **having a token was the decision**: somebody adding one to try a search moved every flight on the site onto it, silently. Where the bookings go is a business decision and it takes somebody deciding. Flights now go to **Kayak**, and hotels to **Booking.com**, unless an environment variable says otherwise in as many words: `DUFFEL_FLIGHTS=1` and `DUFFEL_STAYS=1`, both of which also need the token. Either alone is not a decision — a token with no flag is somebody who has an account, and a flag with no token is a search that cannot run. Only the exact string `1` counts, so a guess at the format (`true`, `yes`) fails safe rather than moving the bookings. Nothing Duffel was removed: it is one variable away, and the flight review, the booking endpoint and the key test all still work. **The admin now says where searches go** at `/admin/settings/connections`, naming the variable **in both directions** — the person who needs it is the one looking at it while flights go to Kayak and wondering how to change that, and an earlier version hid it from exactly them. |
| Earning on the searches (owner request, not in either scan) | **Done** | Booking.com turned the site down through Commission Junction, so the direct programme was closed off; a Travelpayouts account covers Booking.com, flights and cars under one marker instead. **A marker on its own earns nothing**, and the site was proof of it: `TRAVELPAYOUTS_MARKER` was read from the environment, carried into `/book` and typed into the `Affiliate` object — and applied to no link at all. Travelpayouts is paid by **owning the redirect**, so a search has to be sent through tp.media rather than merely tagged. `/admin/settings/earnings` takes one link per search, pasted straight out of the dashboard's link builder — no programme number to copy by hand, since those differ per account and a wrong one is invisible. **Each link is checked against the partner that search really opens** and refused if it disagrees: a Booking.com link in the flights row is the one mistake with no symptom, because the search still opens, still works, and simply never pays. **A bad setting never breaks a search** — a missing, malformed or wrong-partner link gives back the plain search, because a wrong link costs a commission and a failed search costs a customer. Verified in a browser end to end: a configured hotel search opened `tp.media/r?marker=…&p=…&u=<the traveller's own search>`, and an unconfigured car search opened Kayak unchanged. Alongside it, **car hire was going out untagged entirely** — `CarsForm` was never passed the keys, so every car search earned nothing however things were configured, while the connections screen promised the Kayak key covered "the flight AND CAR searches". A test now scans every hand-off and fails on one that carries no marker or names no search. |
| Sharing a trip: viewer, commenter, editor (owner request, not in either scan) | **Done** | sharing was one thing — you added a person and they could look. Right for a brother-in-law you are showing the plan to, wrong for the two people actually building it with you, who had to send you their changes and wait for you to type them in. A person is now added with **what they can do**: *can view*, *can comment*, or *can edit*. **Nobody was promoted by the change** — everybody already on a trip became a viewer, which is exactly what they could do the day before, and an unrecognised role reads as viewer too. **Every answer comes from the owner's own record, read at the moment of the request.** Nothing is taken from the browser but the share token: the browser is precisely where somebody would claim to be an editor. Somebody not on the trip gets the same 404 as a link that does not exist, so the endpoint cannot be used to find out whose links are real; somebody who *is* on it and cannot edit is told so in words rather than lied to about the trip existing. **An editor's save cannot delete the owner's documents** — they are served the trip with every boarding pass stripped out, so what they send back has none, and saving it as-is would wipe the owner's own. Each item keeps whatever attachments its own record held. Re-sharing with somebody already on the trip **changes their role instead of adding them twice**, which is also how an editor is put back to a viewer. Sharing with no role is refused: whoever is sharing decides, once, in front of them. **Notes on the trip now exist**, which is what makes the commenter role mean anything. Somebody looking at a trip has one of two things to say and neither fits in an edit: “the hotel is forty minutes from the shul” is a fact the planner did not have, and “can we not do Uman on the Friday?” is a question. Both used to leave the site as a WhatsApp message and come back as the owner retyping something. A note can be **attached to the stop it is about** rather than landing in a list at the bottom with nine others. **Dealt with, not deleted:** a note is marked done and stays, because what somebody raised and that it was answered is worth more than a short list. **The owner can mark any note done and can delete none of them** — a trip's owner removing somebody's question rather than answering it is the one thing that would stop people writing them; only the author can delete their own. One collaborator cannot close another's question either. Notes are keyed to the **owner**, not the share link, so turning sharing off and on again does not take them with it. **The owner is emailed when somebody leaves a note**, with the note's own words in it — “you have a new note” makes somebody open a page to find out something that would have fitted in the sentence they just read. Not when he wrote it himself, and not at all when he signs in with a phone number, which has nowhere to send it. **Best effort, always:** the note is stored before the email is attempted and stays stored whether or not it sends. Losing somebody's question because a provider was down would be the worst possible trade — checked by running the whole flow against a deliberately invalid mail key and watching the note survive a real 401. **Still open:** an editor opens the trip through the read-only shared view rather than the planner, and nobody but the owner is told. |
| Going live: the notice on the first visit (owner request, not in either scan) | **Done** | the site is going live before it is finished, which is the right way to launch and means the first visitors will meet a phone number nobody has rung in a year. A **modal on the first page somebody lands on** says so: what is still being built, what to check, and where to write in. Shown **once** — one that reappears teaches people to dismiss things unread, including the next one. The wording matters more here than on most sites: people plan real journeys on this, and “use with caution” that does not say what to be cautious about is decoration. So the notice names the things people actually rely on — a shomer's number, an address, a time to be somewhere — and **the admin refuses to save a caution that does not**. Every word is editable at `/admin/settings/website`, along with the switch that ends it, because the day this stops being true the owner must be able to turn it off himself. A **version** field makes everybody see it again when the wording changes, since somebody who agreed to the old words has not agreed to the new ones. Never over the admin or a sign-in box — the admin is his workshop, and a modal over a password box is an obstacle. With no store connected it still shows, with the built-in wording, because a site that is new is new whether or not Redis is up. |
| The tab icon (owner request, not in either scan) | **Done** | six goes at this, and the first five were each rightly complained about — a navy tile behind it, a gold smudge, the compass rose standing in for the whole mark, and then a hand-drawn hand-and-compass, first in navy and then in gold. The last two were legible and were **not the mark**. It is now built from the artwork itself: `public/logo-mark.png` is the logo, and `scripts/build-icons.mjs` makes every icon the site ships from it. What lets fine line-work shrink is not redrawing it but two steps that keep it — **the white is lifted by solving for coverage** rather than keyed out, so edges get a real alpha instead of the pale fringe a smudge is made of; and **the ink is thickened before shrinking**, more the smaller the frame, so a stroke has a pixel to land on. The tab icon is framed a little tighter than the app icons, dropping the “N” above the compass, which is the sparsest part and the first to vanish anyway — losing it lets everything else be bigger. At 32px and above it reads clearly as the gloved hand holding the compass, which is what a modern display actually draws; at 16px it is soft, and that is the honest limit of the artwork rather than a choice. Tests check the artwork is in the repo, that the redrawn SVG has not come back (a browser prefers an SVG over everything), that every frame is RGBA and really transparent, and that the frames are all the same shape. |

| Signing in with Google (owner request, not in either scan) | **Done** | one button instead of an email and a password. **The owner's decision, and the whole of it: it is the SAME ACCOUNT.** Somebody who signed up with a password and later presses the Google button lands in the account they already have — their trips, their notes, their boarding passes — having skipped typing the password. Not a second account and not a second identity. **The one thing that makes that safe is `email_verified`.** Linking by address means whoever proves they hold an address gets the account behind it, so the proof has to be real, and Google's is not always — a Workspace account on a custom domain can carry an address nobody confirmed. Those are **refused outright** and told to use their password, because accepting one would hand a stranger somebody else's saved trips and look exactly like an ordinary sign-in. Every other claim is checked too: issued for this site and not another, issued by Google, not expired, and answering the sign-in this browser actually started. **A Google sign-in also verifies the account** — an account still waiting on our six-digit code is verified by it, because Google checking the address is better evidence than a code sitting unread in the same inbox, and the pending code is spent rather than left live. **An account opened by Google has no password**, not one nobody chose: the hash is random bytes that nothing can match, and "forgotten password" sets a real one by the ordinary path. **Off unless both keys are set**, and off is the right default — a half-configured button sends somebody to a Google error page. Asks for the address and the name and nothing else, and asks for no offline access, so no long-lived credential is kept for a site that does nothing while you are away. Setup is step 9 of docs/setup-walkthrough.md. |
| Pro and Business accounts (owner request, not in either scan) | **Done** | an account is now a **Traveler**, **Pro** or **Business**, and a person can ask for a different one from their own account page. **Nothing is behind any of them, deliberately** — what Pro eventually includes is the owner's to decide, and writing it down now would put promises on the website nobody has agreed to keep. So the rule the code holds to: **a plan never decides what anybody can do.** Nothing reads it to allow or refuse anything, and a test says so, so wiring a gate to a plan fails a build rather than quietly taking something away from a traveller who had it yesterday. **And nothing is charged** — there is no payment anywhere on this site, no card is taken, no subscription starts, and both the account page and the admin queue say so in as many words rather than looking like a checkout that is not one. Asking records interest and tells the owner; it never grants itself. One open request per account, so somebody who changes their mind is one person waiting rather than three. A Business request has to name the business, checked on the server as well as the screen — the point of one is that it belongs to a business somebody can find in the directory. The owner answers at `/admin/accounts`, and can also set any account's plan directly from the list, which is the only way one comes back off. Granting **sets the plan first and marks the request second**, so the worst case is an account on the right plan with a request still showing open. Every account made before this is a Traveler with no field written, which is exactly what it was. **Still open:** what Pro and Business are actually for. |
| 23. What you typed but had not saved | **Done** | the history above covers a save that went wrong. It could not cover the twenty minutes that never became a save at all: the forms write on the button and not before, so an overview somebody spent an evening on lived nowhere until they pressed it, and a closed tab took it with nothing left to say it had existed. What is typed is now kept **in that person's own browser** — never sent anywhere, never in the database, never seen by anybody else — and offered back on the next visit: "You typed something here 8 minutes ago and did not save it", with **Put it back** or **Throw it away**. It is thrown away by itself the moment a save succeeds, and after a week, because week-old words put back over a record somebody else has since edited would be a quiet overwrite rather than a rescue. **Nothing is put back without being asked** — the page shows what is actually saved until the button is pressed. **A draft is kept only while the form differs from what it was loaded with**, so typing a letter and deleting it leaves nothing behind and nobody is offered work they do not have; clearing a field on purpose is a change and is kept. **A draft never carries which record it is** — not the slug, not the id, and not React's own hidden action fields, which name which action a Save runs and what is bound to it. That is the one mistake here that could damage something other than the form it came from, so it is refused on the way in as well as on the way out: storage is an outside thing, and an older version of this code may have written what is in it. Each form on a screen keeps its own, including **the add-a-contact and add-a-listing forms, which are the ones with nothing in the database to fall back on**. **Still open:** the same net on the page and itinerary editors. |
| Recently deleted (owner request, not in either scan) | **Done** | every "Remove" in the admin sat next to "Edit" and was final. A shomer's number took a phone call to get; one mis-tap and there was nothing anywhere that said it had ever existed — the row was simply not in the list, and nobody could tell a deletion from something never entered. A deletion now leaves the record behind for **30 days** at `/admin/recycle`, with the day it happened, how long is left, and a way to put it back **as itself** — the same id, not a copy, so nothing that referred to it is looking at a different row. Covers contacts, shomer numbers, kevarim, listings, businesses and advertisements. **The record is written back first and taken out of the bin second**, because the other order leaves a moment where it exists nowhere at all; the worst case is a stale entry, and pressing it again says "already there" rather than making two. A kever whose beis hachaim was deleted afterwards is refused in plain words and stays in the bin. The bin lives in the private store rather than the database — the point is to survive a mistake made IN the database. **Pictures are deliberately not kept**: a photograph is stored as the picture itself, and a month of deleted ones would fill the store; the screen says so rather than leaving somebody to find out. **Still open:** undo for an edit, as opposed to a deletion. |
| 17. Travel documents and confirmations | **Part** | **Scoped down by decision: no passport or identity-document storage**, and the upload says so rather than only being told not to. What a traveller genuinely hands over now lives on the stop it belongs to: a **boarding pass** on the flight, a **ticket** on the stop, a **booking confirmation** on the hotel. PDF or a picture, up to 600KB, six per item; SVG refused for the same reason the hechsher marks refuse it. **A boarding pass is treated as sensitive**, because a full name and a booking reference are enough to change a flight: it is kept in its own store with the uploading account's email against it, served only to that account, never cached, sandboxed by CSP, and refused with the same 404 for "not yours" as for "not there". It is **stripped entirely from a shared trip** and never rendered in the printed copy — the reference alone would fetch nothing, but the person holding the link is not even told a pass exists. Uploading needs an account, and the reason is given: 600KB in a browser's own storage breaks the trip it was meant to help. **Every document on a trip is now readable in one place**, by the day it is needed rather than by which stop it was filed on — the planner's shape is right for building a trip, and the wrong one at half past five at the airport. On the command centre as "On your phone", grouped by day with today marked; and on the trip strip in the planner, today's passes as chips one tap from the top of the page. A flight's pass is filed under the day it **leaves**, not the day it lands: a red-eye is boarded on the 10th and a pass filed forward is a pass on the day after it was needed. Anything on a stop with no date is listed under its own heading rather than dropped or guessed at. **Still open:** offline access on the day. |
| 24. Security | **Part** | rate limiting on every account endpoint, one password rule, terms consent, the six-digit code never reaches the browser. an admin grant can be narrowed to some of five areas (§18), checked on the server per screen. an edit history now records what changed and who changed it (§23). Missing: admin 2FA, backups. |
| 1. Responsive admin foundation | **Done** | `npm run audit:admin` — 17 screens at 320/375/390/768/1024/1280. No sideways scrolling, no table breaking out of its box, no unlabelled input, every control 44px. |
| 2. Simplified admin navigation | **Part** | sections and global "go to" search already existed. Added: breadcrumbs, a named back link, quick-add, recently visited. Still open: pinned sections, draft/published filters. |
| 3. Admin dashboard homepage | **Part** | content totals and quick-add actions added, on top of the alerts, work panels and completeness queue already there. Still open: broken links, missing images, expired ads, API health, popular destinations/routes. |
| Map (owner request, not in either scan) | **Done** | opens on everything the site holds — 287 places — instead of Kraków at 50 km. Things to do and places to stay added alongside kevarim, kosher food and airports. Counts per category for whatever area you are looking at. Markers are the logo's eight-point compass rose, shrinking at continent zoom so three hundred of them stay separable. **Open:** clustering, so a hundred places in one region become one pin with a number. |
| 4. Complete destination manager | **Part** | thirteen sections instead of seven — Tefillos, Shabbos, hospital, emergency, kosher shops and parking now exist end to end, from one shared list that the editor, the completeness tracker and the public page all read. Each listing carries how far it has been checked, when, and where it came from. The completeness tracker now counts what is in the database, so entering a hospital or a Shabbos note actually moves the number — a seeded town went 33% → 58%, with nothing left unanswerable. Photos can be uploaded (§7). |
| 7. Rich media library | **Part** | pictures at all three levels a traveler asks about — the **town**, the **beis hachaim**, and **one listing** (this hotel, this shul, this mikvah). Uploaded, captioned, credited and published from the admin; on the page with the credit under them. A picture with no credit is saved as a draft and says so, enforced on the server as well as the screen. A built-in beis hachaim needs no database row first — the first picture creates it, the way a shomer's number does. Removing a listing removes its pictures and nothing else. **Visitors can send one in, and the owner confirms** — that is the whole workflow, per his decision: the public endpoint can only ever write a draft, and the one screen that can publish is his. A submission has to name whose photograph it is AND separately confirm the sender may share it, because those are two different questions. Five pictures an hour per address, counted on storing rather than on trying, so a mistyped email does not lock somebody out. **Still open:** reordering, and a shared library rather than per-owner uploads. |
| 15. Personal Travel Command Center | **Part** | `/command-center` — a signed-in traveller's trip, stop by stop, arranged as what to DO in the week before they go: the numbers worth having in their phone, then the stops that need something, then the rest. A beis hachaim stop is judged against its own record, so "nobody on file to let you in" and "the exact grave has not been checked" are real answers rather than a blank. Deliberately quiet: only what can stop the visit happening earns a line, because a list that says everything is a list nobody reads. **Still open:** dates and documents, alerts (§16), anything that pushes rather than waits to be opened. |
| 5. Cemetery and tzaddik management | **Part** | every tzadik has a page — `/tzaddikim/[person]` — with his seforim, his yahrzeit, who else is buried there, and how to reach the kever. Press a name on a beis hachaim page and you are on it. The record is still the cemetery's, read from the other end, so a correction in the admin shows on both. 327 people, addressed by name and disambiguated by town only where two really share one. **Still open:** the reusable tzaddik record in the admin, and a person buried in more than one recorded place. |
| 16. Command Center alerts | **Part** | the trip says two things without being asked, at the top of the command centre. **Shabbos first** — a stop planned for Shabbos, or on erev Shabbos with the local candle-lighting time worked out from that stop's own coordinates (lib/shabbos.ts already did the sunset arithmetic; this calls it per stop). It leads not because it is likeliest but because finding out late is a different order of problem: a missing shomer number costs a morning, a kever visit planned for Shabbos makes somebody choose between the plan and the day. Then **leaving soon with something unresolved**, urgent inside a fortnight and quiet beyond it. A trip with no dates says so plainly, because silence there reads as "no problems" when it means "nothing was checked". **Still open:** anything that pushes — email or a notification — and a trip-level roll-up of the travel advisories (they already show per country). |
| 19. Advertisement manager | **Part** | the data was always rich — advertiser, placements, devices, paths, dates, priority, views, clicks. What was missing was the management: every advert read as "enabled" whether or not it was running, and its numbers claimed things they could not support. Each now carries **where it stands** (on the site now / starts later / finished / paused / not placed anywhere — "not placed" beats the dates, because an advert with nowhere to appear cannot be live whatever they say) and **a rate it is entitled to**: below a hundred views it says "3 from 12 — too few to tell yet" rather than 25%, which sounds excellent and means nothing. The list is ordered by what needs doing, and the dashboard names the worst one. Deleting an advert works (#178) — it never had a server branch at all. **Still open:** what an advertiser is owed, a rate over time rather than since forever, and A/B. |
| 18. User and permission management | **Part** | the admin knew nothing about who was in it: one shared password, one shared cookie, identical for everybody who has ever held it. That also made the team screen's grant untrue — marking somebody an administrator put a link to the admin on their account page, and the middleware bounced them to a password prompt they had never been given the password for. **A named administrator can now open the admin as themselves**, from their own account, and the session carries who: the header names them and the sign-in log records "admin account" with the email instead of "admin code" for every person alive. The name cookie is signed, so holding the shared password does not let anybody relabel themselves as the owner. The shared password still works and is labelled as what it is — nobody in particular. **An admin grant can now be narrowed to part of the admin** — words and pictures, places and kevarim, advertisements, money, access and settings — so somebody helping with kevarim no longer also gets the finances, the passwords, every visitor's email and phone number, and the switch that closes the site. Nothing granted still means everything, so no administrator who exists today is locked out by the change; restriction has to be chosen. The check runs on the server in each area's own folder, not in the navigation, and a test walks `app/admin` so a screen added later cannot quietly escape it. **Still open:** 2FA, an audit history of who changed what, and narrowing the API keys the same way. |
| 12. Border crossing management | **Part** | the planner has always said "allow several hours" at an external EU border. That came from one rule — the two countries sit on different sides of the Schengen edge — so it could not name a crossing, could not be corrected when one shut, and could not be improved by the one person who rings ahead. The crossings themselves now exist as data: the road crossings on the borders these trips actually cross, Poland–Ukraine first among them. **What moves is deliberately not in the code.** How long the queue is and whether it is open change by the day, and a number written into the source is a number nobody can correct — so every crossing arrives unchecked, and the site says "nobody has checked this one" rather than guessing. The owner records what was found and **the day they found it**, and a check stops being quoted as current after a month: March's queue length shown in August, as though somebody had just looked, is worse than saying nothing. The route lists the crossings on each border it meets, known-and-usable first, then unknown, then known-and-shut. **Still open:** which crossing a route should actually be sent through, and the same treatment for the advisories. |
| 21/22. What people are looking for | **Part** | the checklist already said what was empty — 111 towns with nothing published, 141 batei hachaim with no checked coordinate — and said the same thing about all of them. Sorted alphabetically, the first afternoon's work went into towns nobody had ever opened. `/admin/reports` sets the visit log against what is actually entered and answers two questions the site could always have answered and never was asked: **which empty pages do people keep opening** (fill those first, most-opened at the top, with a link straight into the editor), and **what did people search for and see nothing** — those may be records that do not exist at all, and no checklist can list a town nobody created. The second is MEASURED, not re-derived: the search box now sends how many results the visitor was actually shown, so a zero is a zero somebody saw rather than a guess made later by re-running the matching. "Add it" carries the term into the add form. **A page with no visits is never ranked** — nobody having opened it is nothing known about it, not a low score, and mixing the two would bury the one page twenty people opened and found empty. Those are counted and left to the checklist. A third list shows where the work has paid. **Still open:** visits over time rather than since-forever, per-country or per-referrer breakdowns, and an export. |
| 8. Suggest-an-edit workflow | **Done** | Review side: a queue ordered oldest-waiting-first, filters by answer, a required reason for turning down or asking for more, append-only history, a link to the page it is about, and a reply draft the owner sends. **Directory submissions arrive on the same form the owner has**, field by field; the review shows only what would change and Accept writes the listing. Corrections to other kinds of content still arrive as prose. |

### Not started

| Section | Note |
| --- | --- |


| 6. Searchable forms and address autocomplete | **Done** | both halves, and the searchable half turned out to be nearly finished: `SearchableSelect` already sits on the only genuinely long list on the site (146 batei hachaim, in the kever and shomer editors), and every other dropdown was measured — 14 pages, 20 hechsherim, a handful of statuses and roles — where a plain `<select>` is the right control and a filter box would be noise. **A test now says so**, failing on any long collection found inside a native dropdown, so the next one cannot slip in quietly. **The address half was a real gap.** Two admin forms still took an address as free text: the kever editor — which is how 146 batei hachaim get theirs — and the content manager. A typed address is one nothing has resolved, and half of these are Polish and Ukrainian street names with diacritics a keyboard gets wrong in a way no page ever notices. Both now pick a real address off the map, **and the coordinate sits beside it instead of in another half of the form with nothing comparing them**. The coordinate is still never filled in automatically, and there is no button to do it: the standing rule here is that an approximate pin at the wrong end of a town is worse than an address and a phone call, and one click is all it takes to turn a guess into a fact somebody drives to. What the address's coordinate is used for is **the check** — a coordinate 178 km from the address it is filed under says so, by name and by distance. A **question, not a refusal**, because the person typing may know where the grave is when the map does not, which happens constantly on these routes. Alongside it the two refusals already proven on airports: something that is not a coordinate, and 0,0 by name, which is what an empty form produces and reads like a real value on a screen. |
| 10. Flight and airport management | **Done** | the site knew 99 airports and 6 city groups, and both were written into `data/airports.ts` — right for the ones that will not change, and wrong for the ones this site actually needs: a regional strip an hour from a kever town, a field that only matters in Elul. Every one of those was a code change. `/admin/airports` now adds them, and the same shape as the border crossings: **the built-in list stays a file** — readable, reviewable, impossible to damage from a form — and what the owner adds is merged on top. **Saving a code that already exists corrects it rather than doubling it**, which is how a wrong coordinate or a missing spelling gets fixed; two rows for JFK would leave the traveller guessing. Removing a correction puts the built-in one back. **A coordinate is required and is checked as a real place on earth** — 0,0 is refused by name, because it is what an empty form produces — since an airport the planner cannot measure a driving time to would be added and then quietly do nothing. **City groups are the substantive part.** Somebody flying to a city with two airports usually does not mind which, and a group is offered above the individual ones. **A group of fewer than two is refused**, and says what to do first: a group holding one airport is a duplicate row in the picker saying the same thing twice. Built-in groups are marked as such and carry no Remove — a button that appeared to delete one from a file would be lying. The picker fetches the additions **once for the whole page** and never waits for them: the built-in list is in the bundle and answers instantly, so a failed request leaves the box working with the 99 it shipped with. |
| 11. Route planner management (admin) | **Done** | every judgement the planner makes about a day came out of numbers written into two source files, and there was nowhere in the admin that even said what they were. A day was fourteen hours, 08:00 to 22:00. A stop nobody had timed was ninety minutes, a beis hachaim included. Driving was 22/38/58/88 km/h with roads 1.35× the straight line. **Those are opinions about somebody else's traveller** — a group that davens before it drives and wants to be off the road before maariv does not have a fourteen-hour day, and the man who drives the Lizhensk road knows it better than the guess does. `/admin/planner` now sets all thirteen. **The built-in figures are exactly what the code did before**, so turning this on changed nothing; only changing a number changes anything, and only what he changes is stored — a later correction to a built-in figure still reaches every trip that never overrode it. **The worked example is the point:** four real legs, one per speed band, recomputed as you type — change the motorway speed and Lizhensk → Uman moves from 9 h 11 m to 13 h 23 m before anything is saved. **A figure that would break the arithmetic is refused by the consequence, not the rule** — a speed of zero is "a drive that never ends", speeds out of order is "a long drive would come out quicker than a short one", which is a typo nobody would ever see in the result. **The planner, the shared link and the printed copy now read the same set**; the shared link was reading neither these nor the border crossings, so one trip gave one set of driving times on the screen it was built on and another on the link sent to the person actually driving it. Found in a browser: the reset button was a `<form>` inside a `<form>`, which the browser drops — it would have submitted the save it was meant to undo. |




| 20. API management | **Done** | there were two dozen environment variables behind this site and no screen saying which were set. That is a bad way to run anything and a worse way to go live: a missing key here does not throw, it makes one feature quietly stop working, and nobody notices flight lookup is dead until somebody types LO282 and gets nothing back. `/admin/settings/connections` now lists all 22 connections with **what stops without each one** — “a traveller typing a flight number gets nothing back”, not “AERODATABOX_API_KEY is not set”, because the second tells nobody anything. Sorted worst-first, and the summary counts **only the essentials**: a checklist that says six things are missing when five are optional teaches somebody to ignore it. **SET IS NOT WORKING, and it says so.** All this can see is whether a variable has a value; whether the key is valid or the account is in credit is a question only a real request answers, which is what the test buttons below it are for. **No value ever reaches the browser** — only emptiness is read, checked by fetching the page with a password and an API key set and finding neither in the HTML. The guard that keeps it honest is a test that **reads the source for every `process.env.X`** and fails on one nothing describes. It found eight on the first run, including all three **affiliate keys** — the ones that decide whether the site earns anything, and whose absence changes nothing a visitor can see. |
| 23. Publishing workflow | `ContentStatus` already has DRAFT / PUBLISHED / NEEDS_REVIEW. No revisions, autosave or scheduling. |
| 25. Website settings | **Done** | there was a settings screen with six fields — hero title, hero subtitle, search placeholder, public notice, footer email, booking notice — saving to the database, and **not one of them was read by any page on the public site**. The owner could type a new headline, press Save, reload the front page and see the old one. That is worse than having no settings screen at all: it makes somebody believe he has already fixed something he has not, and there was nothing in the code connecting the two ends so nobody could notice. Meanwhile the contact address was typed into **five components separately**, one of them twice, plus the terms, the privacy policy and the Lizhensk page — so changing where mail should go meant finding eight places. `/admin/settings/words` now sets nine lines and **every one of them is on the site**: the front page headline and the two lines around it, what the search box invites you to type, the address every form tells people to write to, what a form says once it is sent, the two footer lines on every page, and the booking page's opening paragraph. **Each field says where it appears and links to it**, because the screen it replaces made no claim that could be checked. **The built-in wording is exactly what the site said before**, character for character, so shipping this changed no page; and what the OLD screen had saved is neither merged in silently — it would change his front page today for a decision he made long ago — nor thrown away. It is shown beside what the site is actually saying, with a button to take it. The guard is a test that reads the source and fails on a word nothing renders. **Found by running it:** the root layout did an uncached store read, so every prerendered page on the site — the terms, the privacy policy, every tzadik, every city guide — returned a **500** the moment anything triggered a regeneration. Nothing ever had, so nobody knew. It is cached and tagged now, and one save in the admin changes all of them. |
| 26. AI admin tools | Deliberately last. |

---

## The migration — written, not yet run

The schema can now hold everything the items above were waiting for. The
migrations are on record and were checked against a real PostgreSQL 16; see
**[docs/migrations.md](./migrations.md)** for what they do and how to apply
them.

- Six new `PlaceCategory` values — `TEFILLOS`, `SHABBOS`, `HOSPITAL`,
  `EMERGENCY`, `GROCERY`, `PARKING`. In their own migration, because
  `ALTER TYPE … ADD VALUE` is the one statement here that is not ordinary.
- Verification status and date on `Destination`, `Cemetery`, `PracticalPlace`
  and `DirectoryProvider`; `sources` on `Destination`.
- Consent to publish a personal phone number on `DirectoryProvider` —
  `contactConsent`, defaulting to **false**, with when and how it was given.
- A `Photo` table, with credit and source, draft by default.
- A catch-up migration for four tables that had been added to the schema
  without one: `DirectoryProvider`, `Attraction`, `KosherStay`, `KosherArea`.

**Two ways to run it.** In the admin: Towns → "Set up database & import
destinations", which now brings an older database up to date as well as
creating a new one — checked against a real PostgreSQL 16 from empty, from an
April-era database, and pressed twice, all three landing exactly on the schema.
Or `npm run db:migrate` in a terminal, after one `prisma migrate resolve
--applied 0_init`; that is still the record, and the only one that writes the
migration bookkeeping.

**Also still to do: use it.** This migration only makes room. Reading and
writing the new fields — the destination manager (§4), the provider consent
flags (scan 1 item 12), the photo library (§7) — is the next piece of work,
and `lib/verification.ts` still counts Tefillos, Shabbos, Hospital, Emergency
and Photos as untracked because the *record type it reads* has not changed yet.

---

## Known: one missing table used to erase everything the owner typed

The cemetery page read a beis hachaim and its pictures in a single query. On a
database where the migration had not been run the `Photo` table did not exist,
so that query threw, the catch above it fell back to the built-in record, and
**every kever the owner had added disappeared from the page**. He had saved
them. The admin had said "Saved." They were in the database. The page would
not read them, and nothing anywhere said why.

The town pages were worse: `getPublishedDestinationContent` returned null, so
every listing and every phone number vanished at once and the page fell back to
the built-in content — which looks exactly like a site nobody has entered
anything into. The database import failed for the same reason, on
`destination.createMany`.

The mistake was not the missing migration. It was joining something new and
optional onto something old and essential, so a failure in the new part cost
the old part everything. `lib/db-optional.ts` is the rule now: read the
essential thing on its own, read the new thing separately, and carry on without
it when it fails — saying so in the log, and naming the migration when that is
what it looks like.

---

## Yiddish: a real word, or English

A dozen labels carried a "Yiddish" heading that was an English word spelled
in Hebrew letters — טראַנספארט for transport, דרייווערס for drivers,
דירעקטאָרי for directory, סערוויסעס for services, טור־אָפּעראַטאָרן for tour
operators. To somebody who reads Yiddish that is not a translation; it is the
English word made harder to read, and it says the site cannot tell the
difference. They are gone.

A second pass removed four more the owner marked as not natural — רײַזע פֿירער,
וואו צו גיין, וואו צו שלאפן, פֿאַרבינדונג. Being made of real Yiddish words is
not the same as being what anybody would say, and that is not a distinction
this codebase can make for itself.

What is left is what people say: כשרות עסן, מנינים, מקוה, אכסניא, בתי החיים,
היים, נסיעות, כשר עסן, and — from the owner — תפילות, שבת, כשר. Place names
and names of tzaddikim are untouched; those were never the problem.

**Hospital, emergency, parking and the four provider categories have no
natural Yiddish**, per the owner. They stay English, and that is the right
answer for them rather than a gap waiting to be filled.

A section with no Yiddish word shows English alone. `tests/yiddish-labels.test.ts`
holds an allowlist, so adding a Yiddish label is a deliberate act rather than
something that creeps back in.

Nothing further is wanted here — the owner has answered, and the answer for
most of them is "there isn't one".

---

## Known: what cannot be made to look like the site

Two browser controls draw part of themselves and will not be styled:

- **A `<select>`'s open list.** The closed control is the site's — cream,
  navy, a gold chevron. The list that drops down is drawn by the operating
  system and no CSS reaches it, in any browser. The only way to change it is
  to stop using `<select>` and build a listbox, which means re-implementing
  keyboard handling, screen-reader announcements and the phone's native
  wheel picker. Not worth it for a menu of four options; possibly worth it if
  a dropdown ever becomes something people stare at.
- **A `<input type="date">`'s calendar popup.** Same problem, solved: on
  anything with a pointer the site draws its own calendar over the top
  (`components/DateField.tsx`), and on a touch screen the native one is left
  alone, because a hand-drawn popover loses to the one built into the phone
  when you are choosing with a thumb.

`components/BookingSearch.tsx` keeps its own date field on purpose: it masks
to MM/DD/YYYY for the partner search forms, and changing the value format
could break an integration that cannot be tested from here.

---

## Known: how long an edit takes to appear

Three different answers, now one. `/directory`, `/attractions`, `/kosher-stays`
and `/map` render per request. The destination, city-guide and cemetery pages
rebuild at most once a minute — measured, not assumed: with a sixty-second
window an edit made after the build does appear.

It used to be an hour on those three, while the admin promised "changes go
live within a minute". Wrong by a factor of sixty, and the shape of a bug
report that reads "the editor does not work".

The difference between the two groups is real and worth remembering: a page
whose reads are `cache: "no-store"` fetches is NOT refreshed by `revalidate`
and needs `force-dynamic`; a page reading through Prisma is.

---

## Known: pages that were frozen at build time

`/directory`, `/attractions`, `/kosher-stays` and `/map` all read content the
owner adds in the admin, and all four were prerendered once per deploy. A
listing added on Tuesday was still absent on Friday — the admin saved it, the
store held it, and the page kept serving the snapshot taken at build. All four
now render per request.

`revalidate = 60` was tried first and measured: the page still never re-read
the store, because those reads are `cache: "no-store"` fetches that a prerender
does not re-run. Worth remembering before adding another owner-edited page —
the mode has to be `force-dynamic`, the same as `/stops` and the admin.

---

## Known: values written into the request address

Several stores save by putting the whole value in the URL —
`set/<key>/<the entire encoded contents>` — instead of in the request body. No
HTTP server accepts an address of any size, so past a certain point the write
simply fails and the screen reports "connect the private database" as though
nothing were configured.

`lib/admin-content.ts` was fixed here because it was already past that point:
with the locations seeded, its save address measured **312,079 characters**, so
nothing in the content manager — settings, locations, accommodations,
suggestions — could ever have saved. It now sends the value in the body, the
same as `lib/hechsher-store.ts`, `lib/media.ts` and `lib/directory-store.ts`
already do.

Still on the old pattern, and small only for now:

- `lib/email-log.ts` — no cap at all, so it grows until it stops saving.
- `lib/expenses.ts` (line 58) — capped at 10,000 items, which is far past the
  point an address stops working.
- `lib/account-store.ts`, `lib/admin-inventory.ts`, `lib/access-passwords.ts` —
  capped or naturally small today.

Each is a two-line change of the same shape. They are listed rather than done
so that this stays reviewable.

---

## Things that need a decision, not code

- ~~**`NEXT_PUBLIC_SITE_URL` is unset.**~~ — **set by the owner.** Because it
  is resolved at BUILD time for prerendered pages, the next deploy has to be
  without the build cache for canonicals and share images to pick it up.
- ~~**Two domains serve the same site.**~~ — **settled.** There is one domain:
  `whitegloveitineraries.com`. The second was a different business's and has
  been given up entirely, so there is no redirect left to get right. The terms
  and privacy pages name this site from a single constant, and a test holds
  that so it cannot drift back.
- ~~**What "Featured" means in the provider directory.**~~ — **decided.** Two
  reasons, recorded per listing in the admin: service found consistently good,
  or sponsored placement. The badge is the same for both and a visitor is not
  told which applies to a given listing. The disclosure does say that some
  featured listings are sponsored — a badge that may mean "they paid" and is
  indistinguishable from an editorial pick is the specific thing the FTC, the
  ASA and EU consumer law are written to catch, so the possibility is stated
  even though the individual listing is not named. If one is ever genuinely
  paid for, a marker on that one is safer still.
- ~~**Account enumeration on password reset.**~~ — **decided: closed.** The
  endpoint now answers identically whether or not the account exists. A
  mistyped address is no longer told it was mistyped, which is the agreed
  cost: telling a typo apart from an unknown address IS the leak.
- ~~**Whether visitors may send in a picture.**~~ — **decided: yes, with the
  owner confirming every one.** Nothing a stranger sends reaches a page by
  itself: the public endpoint can only write a draft, and the only screen that
  can publish is the owner's. The form asks two separate questions — whose
  photograph it is, and whether the sender may share it — because somebody can
  honestly name a photographer and still have no right to hand the picture
  over, and that is most of how a photograph ends up published without
  permission.
- ~~**Storing passport copies**~~ — **decided: no.** The site does not store
  passport copies or identity documents. Per-trip notes exist instead, for the
  things a traveler wants to remember, and the field says plainly that notes
  travel with the trip and are not the place for anything private.
